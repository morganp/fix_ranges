Offical Page is [Amaras-tech.co.uk][amaras]

[amaras]: http://amaras-tech.co.uk/software/fix_ranges

Usage
=====

    $ gem install fix_ranges
    $ irb
    require 'rubygems'
    require 'fix_ranges'
    (0..9).reverse.each {|x| puts x }
    (0..9).reverse_each {|x| puts x }
